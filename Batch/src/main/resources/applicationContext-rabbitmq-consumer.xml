<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="
            http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context
                http://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/rabbit
        http://www.springframework.org/schema/rabbit/spring-rabbit-1.5.xsd">

	<!-- 连接服务配置 -->
	<rabbit:connection-factory id="connectionFactory"
		host="${rabbitmq.host}" username="${rabbitmq.userName}" password="${rabbitmq.password}"
		port="${rabbitmq.port}" virtual-host="/" channel-cache-size="5" />

	<rabbit:admin connection-factory="connectionFactory" />

	<!-- queue 队列声明 -->
	<rabbit:queue durable="true" auto-delete="false"
		exclusive="false" name="RegisterSuccessNotify.queue" />

	<!-- exchange queue binging key 绑定 -->
	<rabbit:direct-exchange name="demoSrping4.queue.exchange"
		durable="true" auto-delete="false">
		<rabbit:bindings>
			<rabbit:binding queue="RegisterSuccessNotify.queue"
				key="${rabbitmq.RegisterSuccessNotifyKey}" />
		</rabbit:bindings>
	</rabbit:direct-exchange>

	<bean id="registerSuccessNotifyListener"
		class="com.fastjrun.demospring4.listener.RegisterSuccessNotifyListener" />

	<!-- queue litener 观察 监听模式 当有消息到达时会通知监听在对应的队列上的监听对象 -->
	<rabbit:listener-container
		connection-factory="connectionFactory" acknowledge="auto">
		<rabbit:listener ref="registerSuccessNotifyListener" method="processUser" queue-names="RegisterSuccessNotify.queue" />
	</rabbit:listener-container>
</beans>